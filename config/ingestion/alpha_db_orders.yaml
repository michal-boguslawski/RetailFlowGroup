name: alpha_db_orders_bronze

job:
  app_name: alpha_db_orders_bronze
  shuffle_partitions: 2

dataset:
  store: alpha
  entity: orders

source:
  type: postgres
  options:
    table: orders
    date_field: updated_at

target:
  layer: bronze
  format: parquet
  mode: append