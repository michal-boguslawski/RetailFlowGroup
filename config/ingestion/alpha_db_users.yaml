name: alpha_db_users_bronze

job:
  app_name: alpha_db_users_bronze
  shuffle_partitions: 2

dataset:
  store: alpha
  entity: users

source:
  type: postgres
  options:
    table: users
    date_field: updated_at

target:
  layer: bronze
  format: parquet
  mode: append