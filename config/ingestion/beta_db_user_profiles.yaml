name: beta_db_user_profiles_bronze

job:
  app_name: beta_db_user_profiles_bronze
  shuffle_partitions: 2

dataset:
  store: beta
  entity: user_profiles

source:
  type: mongo
  options:
    collection: user_profiles
    date_field: createdAt

target:
  layer: bronze
  format: parquet
  mode: append