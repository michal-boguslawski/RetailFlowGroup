store_id: gamma
ids:
  user_id:
    style: integer
  return_id:
    style: integer
    prefix: RET-YYYYMMDD-
  order_id:
    style: integer
  product_id:
    style: integer

minio_bucket_name: retailflow-landing

state_path: data/state/gamma_generator_state.json

pipeline_config:
  null_rates:
    rates:
      user: 0.25
      stock_count: 0.03
      created_date: 0.05
  field_case_corrupt_rates:
    rates:
      discount_code: 0.2
  trailing_spaces_corrupt_rates:
    rates:
      promo_code: 0.1
      discount_code: 0.1
